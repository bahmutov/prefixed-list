'use strict'


/* global describe, it */
const prefixedList = require('.')

describe('prefixed-list', () => {
  it('write this test', () => {
    console.assert(prefixedList, 'should export something')
  })
})
